setup:
  addons:
  - plan: heroku-postgresql:hobby-dev
    as: DATABASE
  config: 
    DATABASE_SSL: true
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - heroku pg:reset
    - for D in ./sql/*; do heroku pg:psql < $D; done