sudo: required

language: node_js
node_js:
  - "8.11.3"

services:
  - docker

before_install:
  - docker build -t my-postgres .
  - docker run --name test-db --env-file .env -p 5437:5432 -d my-postgres
  - docker ps -a